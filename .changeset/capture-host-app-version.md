---
"@fasterfixes/react": minor
---

Add an optional `appVersion` prop to `FeedbackProvider`. Pass the loaded application's build-time release to capture it as `metadata.appVersion` on every submission, including feedback without a selected element.
